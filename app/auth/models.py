"""Models for the auth app.

Django's built-in User model (django.contrib.auth) is used for authenticated
sessions/permissions. TODO: add a profile model here only if AD attributes
need to be cached locally (e.g. display name, department) — keep it minimal.
"""
