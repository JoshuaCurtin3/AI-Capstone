"""Small, generic, domain-agnostic helper functions.

If a helper is specific to one domain (parsing, scoring, AI calls), it
belongs in that domain's own module, not here. TODO: add helpers as concrete,
reused-in-more-than-one-place needs arise — avoid speculative utilities.
"""
