#!/usr/bin/env bash
# Deploy the application to the Ubuntu Server host.
#
# TODO: implement the real deployment steps (pull latest, install deps,
# collectstatic, migrate, restart gunicorn via systemd). This is invoked by
# .github/workflows/deploy.yml — keep both in sync.
set -euo pipefail

echo "TODO: deploy.sh not yet implemented"
exit 1
