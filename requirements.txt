# --- Core framework ---
Django>=5.0,<6.0
psycopg2-binary>=2.9
gunicorn>=22.0

# --- Configuration / environment ---
django-environ>=0.11

# --- Authentication (LDAPS / Active Directory) ---
django-auth-ldap>=4.6

# --- API layer ---
djangorestframework>=3.15

# --- AI integration (explanation generation only, never scoring) ---
anthropic>=0.34

# --- Testing ---
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0

# --- Static analysis / formatting ---
ruff>=0.5
black>=24.0
mypy>=1.10
django-stubs>=5.0
