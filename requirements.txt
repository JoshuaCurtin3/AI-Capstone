# --- Core framework (Phase 2) ---
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
pydantic-settings>=2.4

# --- PostgreSQL / SQLAlchemy / migrations (Phase 5) ---
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# --- Active Directory / LDAPS + signed session cookies (Phase 6) ---
ldap3>=2.9
itsdangerous>=2.2

# --- Testing (Phase 2) ---
pytest>=8.0
httpx>=0.27

# --- Static analysis / formatting ---
ruff>=0.5
black>=24.0
mypy>=1.10

# --- Added in later phases, listed here for visibility only ---
# anthropic                                    (Phase 7 — AI analysis)
# gunicorn                                     (Phase 8 — deployment)
